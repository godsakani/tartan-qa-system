services:
  - type: web
    name: rag-fastapi-app
    runtime: python
    buildCommand: pip install -r requirements-ultra-minimal.txt
    startCommand: uvicorn main_ultra_minimal:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GOOGLE_API_KEY
        sync: false